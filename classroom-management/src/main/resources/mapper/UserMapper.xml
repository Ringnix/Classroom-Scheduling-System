<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zcl.mapper.UserMapper">
    <resultMap id="BaseMap" type="com.zcl.entity.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="role" column="role" />
    </resultMap>

    <select id="findByUsername" resultMap="BaseMap" parameterType="string">
        SELECT id, username, password, role FROM user WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.zcl.entity.User">
        INSERT INTO user(id, username, password, role) VALUES(#{id}, #{username}, #{password}, #{role})
    </insert>

    <select id="findAll" resultMap="BaseMap">
        SELECT id, username, password, role FROM user ORDER BY id
    </select>

    <select id="findById" parameterType="string" resultMap="BaseMap">
        SELECT id, username, password, role FROM user WHERE id = #{id}
    </select>

    <update id="update" parameterType="com.zcl.entity.User">
        UPDATE user SET username=#{username}, password=#{password}, role=#{role} WHERE id=#{id}
    </update>

    <delete id="delete" parameterType="string">
        DELETE FROM user WHERE id=#{id}
    </delete>
</mapper>
